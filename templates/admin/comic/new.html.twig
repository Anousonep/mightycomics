{% extends 'admin/base.html.twig' %}

{% block title %}
    Ajouter un comics
{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="mt-4">Ajouter un comic</h2>
        <div class="row">
            <div class="col-md-3">&nbsp;</div>
            <div class="col-md-6">
                <div class="bg-dark py-3 px-3 mt-5">

                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button type="submit" class="btn btn-primary">Ajouter l'article</button>
                    {{ form_end(form) }}

                </div>
            </div>
        </div>
    </div>
{% endblock %}

